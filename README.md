<img width="1897" height="873" alt="image" src="https://github.com/user-attachments/assets/92ea838f-ae33-439b-9156-54a0d72cbfcd" />
# Pixel Desk — Пиксельный редактор

🎨 Веб-редактор пиксель-арта с поддержкой инструментов рисования, отмены/возврата действий, заливки, экспорта в PNG и переключением светлой/тёмной темы.

---

## ✅ Реализованный функционал

- **Настройка размера сетки** (1–90 ячеек) 
- **Инструменты**:
  - ✏️ Карандаш с выбором цвета
  - 🧽 Ластик
  - 🧹 Очистка всей доски
  - 🪣 Заливка (алгоритм заливки вширину )
  - ↩️ Отмена действия
  - ↪️ Возврат действия
  - 💾 Сохранение в PNG с выбором имени файла
- **История изменений**: каждое действие фиксируется с сохранением предыдущего цвета ячейки
- **Темная/светлая тема** с переключением через кнопку и анимированным фоном
- **Анимации** (анимированные облака в фоне и кнопка переключения темы)

---

## ⚙️ Технологии

- **HTML5** — семантическая разметка, формы, `canvas` для экспорта
- **CSS3** — Grid (сетка пикселей), Flexbox (панель инструментов), `:focus`, `transition`, кастомные анимации облаков
- **Vanilla JavaScript (ES6+)**:
  - Модульный импорт 
  - Работа с DOM, события мыши (`mousedown`, `mousemove`, `mouseup`, `click`)
  - Алгоритм заливки на основе BFS (поиск в ширину)
  - `canvas.toDataURL('image/png')` для экспорта изображения
  - Управление состояниями (`isDraw`, `eraserOn`, `fillModeOn`, `history`, `redoStack`)
- **SVG/иконки** — инструменты реализованы через `<button>` + `title` (можно заменить на `<img>` или SVG)

# Вы можете посетить сайт по ссылке: <https://denisionov1.github.io/pixel_editor/>
<img width="1897" height="857" alt="image" src="https://github.com/user-attachments/assets/b564cba8-3ef2-4618-a8f9-aa2610470cc4" />
